// TODO S3 storage or some such.

pub struct LocalStorage {
    // TODO Just files, not DB.
    // TODO Also some streaming logging option?
}

impl LocalStorage {
    pub fn open(owner: &str, app: &str) -> Self {
        panic!()
    }
}
