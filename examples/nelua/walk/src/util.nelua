local util = @record{}

local s = require "string"
local t = require "taca"

function util.as_bytes(items: auto): span(byte)
  return {data = (@pointer([0]byte))(items.data), size = items:sizebytes()}
end

return util
